export interface HeroDto {
  id: number;
  name: string;
}

export interface HeroListItemViewModel {
  id: number;
  name: string;
}

export interface HeroDetailViewModel {
  id: number;
  name: string;
}
